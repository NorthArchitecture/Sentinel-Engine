# Dependencies
node_modules/

# Anchor build artifacts
target/
.anchor/

# ZK build artifacts (regenerable via setup.sh)
build/

# Circom compiler (local install)
circom/

# Circuit dependencies (regenerable via npm install)
circuits/node_modules/
circuits/package-lock.json

# Accidental "flag" files (mangled shell / Solana CLI can create `--keypair`, `--url`, etc.)
**/--*

# Security - private keys
ma-cle-pro.json
*.keypair.json
target/deploy/*-keypair.json
faucet-devnet-keypair.json
id.json
id/
.env
.env.local
deploy-devnet.mjs

# Frontend & scripts (local / separate tracking)
frontend/
scripts/
.cursorrules

# Landing (optional separate deploy)
landing/
solana-install-init-*

# OS / Finder
.DS_Store
._*

# Test artifacts
test-ledger/

# Personal docs
docs/
.vercel
.env*.local
